import { StyleSheet } from "react-native";

const TextStyles = StyleSheet.create({
  mainBoldText: {
    fontWeight: "bold",
    fontSize: 20,
  },
  largeBoldText: {
    fontWeight: "bold",
    fontSize: 25,
  },
  captionText: {
    fontWeight: 22,
  },
});

export default TextStyles;
